{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for added in app.flashes('added')|slice(0, 1) %}
            <div class="mt-4 alert alert-warning">
                {{ added }}
            </div>
        {% endfor %}
        {{ component('grave_detail', {
            grave: grave,
        }) }}
    </div>
{% endblock %}
