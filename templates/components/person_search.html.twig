<div{{ attributes }}>
    <h3 class="text-uppercase">Wyszukiwanie zmarłych</h3>
    <div class="lead">Wyszukiwanie miejsca pochówku osoby zmarłej</div>
    <small>Pogrubione pola należy obowiązkowo uzupełnić!</small>
    <hr>
    <div class="form mt-3 overflow-hidden">
        {{ form_start(form, {
            'attr': {'class': 'search-form'}
        }) }}
        {% if form.alert is defined %}
            <div class="alert alert-danger" role="alert">
                {{ form_errors(form) }}
            </div>
        {% endif %}

        <div class="row row-cols-1 row-cols-lg-2 row-cols-xl-3 g-2">
            <div class="col">
                {{ form_label(form.surname)}}
                {{ form_widget(form.surname)}}
            </div>
            <div class="col">
                {{ form_label(form.name, null, {
                    'label_attr': {'class': 'fw-bold'}
                }) }}
                {{ form_widget(form.name) }}
            </div>
        </div>
        <div class="row row-cols-1 row-cols-lg-2 row-cols-xl-3 g-2 mt-2">
            <div class="col">
                {{ form_label(form.born) }}
                {{ form_widget(form.born) }}
            </div>
            <div class="col">
                {{ form_label(form.death) }}
                {{ form_widget(form.death) }}
            </div>
        </div>

        <div class="mt-3 float-end float-xl-start">
            {% if last_uri is defined and last_uri != app.request.uri %}
                <button type="button" class="btn btn-secondary" onclick="location.href='{{ last_uri }}'">Powrót</button>
            {% endif %}
            {% if is_granted('ROLE_MANAGER') %}
                <button type="button" class="btn btn-warning" onclick="location.href='{{ path('app_person_not_assigned') }}'">Pokaż nieprzypisane</button>
            {% endif %}
            {{ form_widget(form.search,{
                'attr': {'class': 'btn btn-success'}
            }) }}
        </div>
        {{ form_end(form) }}
    </div>
    <hr>
</div>
