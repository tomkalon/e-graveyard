{% extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-5 mb-3">
        {% include '/person/details.html.twig' with {
            'person': person,
            'last_uri': uri
        } %}
        {% if person.grave is defined and person.grave != null or person.grave != false %}
            {% include '/grave/details.html.twig' with {
                'grave':grave,
                'person': person,
                'people': people,
                'form_add_person': form_add_person,
            } %}
        {% endif %}
    </div>
{% endblock %}
