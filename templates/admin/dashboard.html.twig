{% extends '@EasyAdmin/layout.html.twig' %}

{% block content %}
    <div class="position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center bg-body-tertiary">
        <div class="col-md-10 p-lg-5 mx-auto my-5">
            <h1 class="display-4 fw-normal">{{ 'ADMIN_PANEL'|trans }}</h1>
            <p class="lead fw-normal">{{ 'ADMIN_PANEL_DESC'|trans }}.</p>
            <a class="btn btn-outline-secondary" href="{{ path('app_main') }}">{{ 'HOME_SITE_RETURN'|trans }}</a>
        </div>

    </div>
{% endblock %}